services:
  jekyll:
    image: jekyll/jekyll:4.2.0
    command: jekyll serve --livereload --force_polling --host 0.0.0.0
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
      - ./vendor/bundle:/usr/local/bundle
    environment:
      - JEKYLL_ENV=development
